import { WrapRespomseInterceptor } from './wrap-respomse.interceptor';

describe('WrapRespomseInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapRespomseInterceptor()).toBeDefined();
  });
});
